spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
    bus:
      enabled: false
  rabbitmq:
    listener:
      direct:
        auto-startup: false

jwt:
  secret: verySecretKeyverySecretKeyverySecretKeyverySecretKeyverySecretKeyverySecretKeyverySecretKeyverySecretKeyverySecretKeyverySecretKeyverySecretKeyverySecretKeyverySecretKey
  token-validity-in-seconds: 8400

test-data:
  user-username: testUser
  user-password: testUser

  moderator-username: testModerator
  moderator-password: testModerator

  admin-username: testAdmin
  admin-password: testAdmin
  # TODO: find a way to prevent the tests from trying to connect to rabbitmq